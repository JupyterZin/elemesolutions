<svg viewBox="0 0 400 200" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <linearGradient id="goldGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#D4B85A"/>
      <stop offset="50%" stop-color="#C9A84C"/>
      <stop offset="100%" stop-color="#B8943F"/>
    </linearGradient>
    <!-- Clip for the decorative curved cutout in the L -->
    <clipPath id="lCut">
      <path d="
        M 0 0 L 300 0 L 300 300 L 0 300 Z
        M 52 80 Q 52 145, 95 155 L 52 155 Z
      " fill-rule="evenodd"/>
    </clipPath>
  </defs>

  <g transform="translate(28, 16)">
    <!--
      L — Bold weight serif letterform with curved interior cutout
      The L is heavy, commanding, with a leaf-like negative space curve
    -->
    <g clip-path="url(#lCut)">
      <!-- L vertical stem — bold -->
      <rect x="30" y="18" width="50" height="150" fill="url(#goldGrad)"/>
      <!-- L top serif — left extension -->
      <rect x="18" y="18" width="22" height="6" fill="url(#goldGrad)"/>
      <!-- L top serif — right small -->
      <rect x="80" y="18" width="10" height="6" fill="url(#goldGrad)"/>
    </g>

    <!-- L foot (horizontal bar) — extends right to connect toward M -->
    <rect x="30" y="162" width="130" height="6" fill="url(#goldGrad)"/>
    <!-- L bottom-left serif -->
    <rect x="18" y="156" width="18" height="12" fill="url(#goldGrad)"/>
    <!-- L foot right serif tick -->
    <rect x="155" y="156" width="8" height="6" fill="url(#goldGrad)"/>

    <!-- Curved cutout overlay — the signature leaf/wave shape inside the L -->
    <path
      d="M 80 68 Q 80 155, 148 162 L 80 162 Z"
      fill="url(#goldGrad)"
      opacity="0.92"
    />

    <!--
      M — Thin/hairline weight serif letterform
      The contrast with the bold L creates the luxury editorial feel
    -->
    <!-- M first leg — thin -->
    <rect x="160" y="18" width="3.5" height="150" fill="#C9A84C"/>
    <!-- M first leg top serif left -->
    <rect x="150" y="18" width="18" height="5" fill="#C9A84C"/>
    <!-- M first leg bottom serif -->
    <rect x="150" y="162" width="18" height="5" fill="#C9A84C"/>

    <!-- M left diagonal — thin -->
    <polygon points="163.5,23 167,23 218,155 214.5,155" fill="#C9A84C"/>

    <!-- M right diagonal — thin -->
    <polygon points="214.5,155 218,155 269,23 265.5,23" fill="#C9A84C"/>

    <!-- M center point — small diamond accent where diagonals meet -->
    <polygon points="214,152 218,162 222,152 218,142" fill="#C9A84C" opacity="0.6"/>

    <!-- M last leg — thin -->
    <rect x="265.5" y="18" width="3.5" height="150" fill="#C9A84C"/>
    <!-- M last leg top serif -->
    <rect x="261" y="18" width="18" height="5" fill="#C9A84C"/>
    <!-- M last leg bottom serif -->
    <rect x="261" y="162" width="18" height="5" fill="#C9A84C"/>
  </g>
</svg>
